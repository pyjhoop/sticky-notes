# design/

**읽기 전용.** 이 폴더의 파일은 수정하지 않는다.

## `StickyNote App.dc.html` — 버전 2 (2026-08-03, 현재 디자인 원본)

**색·폰트·치수의 최종 근거는 이제 이 파일이다.** 사용자가 "디자인이 이상하다"며 전면 시각
개편을 요청해 받은 새 디자인. `Sticky Notes for Windows.dc.html`(버전 1)을 대체한다.

렌더된 화면을 보려면:
https://claude.ai/design/p/be9035d8-3aeb-4973-9012-aecd19ec8dc1?file=StickyNote+App.dc.html

버전 1 대비 바뀐 것 (2026-08-03 사용자 결정):
- **폰트**: Noto Sans KR/JetBrains Mono → **IBM Plex Sans KR / IBM Plex Mono**. 앱 전체 통일
- **색 체계**: hex → **oklch()**. 톤이 더 차분한 웜 그레이 계열로 전면 교체
- **메모 팔레트 5색 → 6색**: 노랑·분홍·파랑·초록·보라 → **노랑·라임·민트·블루·라벤더·화이트**
  (DB `notes.color` 인덱스 재매핑 필요 — 매핑 근거는 `process.md` 계약 변경 이력)
- 관리 창(옛 "보드 창") · 설정 창 · 트레이 메뉴 · 업데이트 알림 토스트 레이아웃이 새로 그려짐
- 언어 전환(한국어/English) · 자동 백업 토글+경로 · "모든 메모 숨기기" 단축키는 디자인에는
  있지만 **이번 개편 범위에서 제외** (사용자 지시 — 시각 개편만, 새 기능은 나중에)

이 파일도 브라우저에서 바로 열리지 않는다 — Claude Design 런타임(`support.js`, `<x-dc>`)에 의존.

## `Sticky Notes for Windows.dc.html` — 버전 1 (역사적 기록용)

**더 이상 디자인 원본이 아니다.** 색·폰트·팔레트는 위 버전 2를 따른다. 남겨두는 이유는
옵시디언 동기화 제외 등 **v1 범위 결정의 배경 설명**이 여기 있고, 레이아웃 근거(체크박스·
코드블록·위키링크 렌더 규칙 등 버전 2가 다시 그리지 않은 부분)가 여전히 유효하기 때문이다.
버전 2와 색·폰트가 다르게 나온다면 **버전 2가 옳다.**

디자인 원본. 색·치수·문구의 **최종 근거**다. 구현 중 애매하면 여기를 본다.

이 파일은 브라우저에서 바로 열리지 않는다 — Claude Design 런타임(`support.js`, `<x-dc>` / `sc-for` / `sc-if` / `{{ }}`)에 의존한다. 로컬 사본은 **소스로 읽는 용도**다.

렌더된 화면을 보려면:
https://claude.ai/design/p/5a69a538-0b47-467b-90a1-3ee91ffde43f?file=Sticky+Notes+for+Windows.dc.html

## 어디를 보면 되는가

| 찾는 것 | 위치 |
|---|---|
| 팔레트 5색 (종이/크롬) | 하단 `<script>` 의 `get colors()` |
| 컨트롤 바 레이아웃·아이콘 | `<!-- 프레임리스 호버 컨트롤 바 -->` |
| 마크다운 렌더 스타일 (체크박스·코드블록·위키링크·태그) | `<!-- 본문 · 마크다운 라이브 프리뷰 -->` |
| 저장 푸터 | `<!-- 동기화 푸터 -->` |
| 리사이즈 그립 | 활성 메모 블록 마지막 `cursor:nwse-resize` div |
| 보드 창 (타이틀바·검색·필터·카드) | `<!-- 메모 보드 창 -->` |
| 설정 창 | `data-screen-label="Settings"` |
| 유휴 / 충돌 / 트레이 메뉴 상태 | `STATES` 열 |
| 트레이 메뉴 항목 | `<script>` 의 `trayItems` |

## 주의 — 디자인과 v1 구현의 차이

디자인은 옵시디언 양방향 동기화를 전제로 그려졌지만 **v1은 SQLite 단독이다.**

- 동기화 관련 UI(푸터 `Vault / …`, 보드 `Vault 동기화`, 카드 `동기화됨`/`로컬 전용`/`동기화 대기`, 트레이 `지금 볼트와 동기화`, 설정 `OBSIDIAN` 섹션)는 **레이아웃을 유지하고 의미만 바꾼다**
- **충돌 상태(`볼트 버전이 더 최신`)는 구현하지 않는다**
- `fonts.googleapis.com` 링크는 옮기지 않는다 — 폰트는 로컬 번들

정확한 대응표는 `plan.md`의 "디자인 대비 변경점" 절.
