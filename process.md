# process.md — 작업 절차

> **새 세션은 이 파일을 먼저 읽는다.** `CLAUDE.md`(규약) → `plan.md`(무엇을) → 이 파일(어떻게) 순서.
>
> **새 세션은 리더(오케스트레이터)다.** 직접 코드를 쓰지 않고 에이전트를 스폰해서 개발·검증시키고, 개발이 완료될 때까지 루프를 돈다. 아래 **실행 모델** 절이 핵심이다.

---

## 진행 상황

| 단계 | 트랙 | 상태 | 브랜치 | 마지막 갱신 |
|---|---|---|---|---|
| 문서 3종 + 디자인 사본 | — | ✅ 완료 | — | 2026-07-26 |
| **M0** 스캐폴드·계약·스파이크 | — | ✅ 완료 (검증 PASS) | `main` | 2026-07-26 |
| **CI** main 푸시 → 빌드 + 태그 릴리스 | — | ✅ 완료 — `v0.1.2` 릴리스 확인 | `main` | 2026-07-26 |
| M2 백엔드 | A | ✅ **완료 — 검증 PASS, main 병합** | `track/a-backend` | 2026-07-26 |
| M3 에디터 | B | ✅ **완료 — 검증 PASS, main 병합** | `track/b-editor` | 2026-07-26 |
| M1·M4 메모 창 | C | ✅ **완료 — 검증 PASS, main 병합** | `track/c-note-window` | 2026-07-26 |
| M5·M6 보드/설정 | D | ✅ **완료 — 검증 PASS, main 병합** | `track/d-board-settings` | 2026-07-26 |
| **통합 게이트** | — | ✅ **완료 — 검증 PASS** (스텁 잔존 0, 실행 확인) | `main` | 2026-07-26 |
| M7 첨부·패키징 | — | ✅ **완료 — main 병합** (코드 검증만) | `m7-attachments` | 2026-07-26 |
| **사용자 신고 결함 2건** (투명도 실시간 반영 · 메모 바깥 검정 영역) | — | ✅ **완료 — 실기 확인 PASS** | `main` | 2026-07-26 |
| **사용자 신고 5건** (커서·프로세스 유지·보드 갱신·자동 업데이트·이미지 리사이즈) | — | 🟡 **코드 완료 — 사용자 실기 확인 대기** | `main` | 2026-07-26 |
| **캐럿 가시성 재발** (신고 #1 재발) | — | 🟡 **검증 PASS · main 병합 — 사용자 실기 확인 대기** | `fix/caret-visibility` | 2026-07-26 |
| **스크롤바 축소** | — | 🟡 **main 병합 — 사용자 실기 확인 대기** (검증 생략) | `fix/scrollbar` | 2026-07-26 |
| **코드블록 언어별 스타일 · 줄 맨앞 캐럿 · 현재 줄 강조 제거** | — | 🔵 진행 중 | `fix/editor-polish` | 2026-07-26 |

> **2026-07-26 사용자 지시 — 이 세션 한정, 검증 에이전트 생략.**
> 속도 문제로 구현 → 검증 루프의 **검증 단계를 건너뛴다.** 구현 에이전트가 `npm run build` · `npm test` 를
> 자체 통과시키면 리더가 바로 병합한다. **진행 표·계약 변경 이력 갱신은 그대로 한다.**
> 다음 세션은 이 지시를 승계하지 않는다 — 기본값은 `process.md` 실행 모델(검증 필수)이다.

상태 기호: ⬜ 대기 · 🔒 선행 미완 · 🔵 진행 중 · 🟡 사람 확인 대기 · ✅ 완료 · ⚠️ 막힘

> **2026-07-26 사용자 지시 — 중간 사람 확인 생략.**
> 🟡 사람 확인 항목(스파이크 1·2번 포함)을 **중간에 사용자에게 묻지 않는다.** 진행 표에 쌓아두기만 하고
> 리더는 M0 → M7까지 자율로 끝까지 돈다. 사용자가 **전체 완료 후 한 번에** 직접 테스트한다.
> 아래 "멈춰야 할 때" 표에서 **"사람 확인 항목이 쌓임" 행은 무효**다. 나머지 에스컬레이션 조건(수정 3회 FAIL,
> 계약 변경 요청, 스파이크 **기술적** 실패, 메모리 게이트 초과)은 그대로 유효하다.
> 트랙 C 차단 해제는 사람 눈이 아니라 **plan.md의 1순위 경로(CSS opacity + CSS radius + DONOTROUND + CSS drop-shadow)**
> 채택을 기본값으로 삼아 해제한다. 구현·빌드 단계에서 기술적으로 불가능하다고 판명되면 그때 폴백을 검토한다.

**M0 스파이크 결론** (M0 완료 시 반드시 채울 것):

```
투명도 구현 방식:  [x] CSS opacity   [ ] 네이티브 SetLayeredWindowAttributes
라운드 코너:       [x] CSS radius + DONOTROUND   [ ] DWM ROUND
그림자:            [x] CSS drop-shadow + 24px 여백   [ ] DWM 기본 그림자
비고: plan.md 1순위 경로 전량 채택. 폴백 불필요.
  · 35%에서 뒤 창이 비치고 본문 판독 가능, 아티팩트 없음 (스크린샷 확인)
  · DwmGetWindowAttribute(33) == 1(DWMWCP_DONOTROUND), 코너 6배 확대 시 검은 테두리 없음
  · drop-shadow가 사방 24px 여백 안에서 잘리지 않음
  · 네이티브 폴백 set_window_opacity는 win.rs에 구현돼 있으나 **사용하지 않는다** (경로 유지만)
  · 스파이크 4(드래그/리사이즈 깜빡임)만 미판정 — 사람 확인 항목으로 이월
```

**트랙 C는 이 블록이 채워지기 전에 스폰하지 않는다.** → 2026-07-26 채워짐, C 차단 해제.

---

## 실행 모델 — 리더 세션

### 리더가 하는 일

1. 다음에 실행할 단계를 진행 표에서 판단한다
2. **구현 에이전트**를 스폰한다 (병렬 가능하면 한 메시지에서 동시에)
3. 구현이 끝나면 **검증 에이전트**를 스폰한다 — 구현한 에이전트와 반드시 다른 에이전트
4. 검증 실패 → **수정 에이전트**를 스폰한다 → 3번으로 돌아간다
5. 검증 통과 → **커밋 + 푸시 + `main` 병합 + 푸시** → 진행 표 갱신 → 다음 단계로
6. 사람만 확인 가능한 항목이 쌓이면 사용자에게 묶어서 요청한다

**M0부터 통합 게이트까지 이 루프를 자율적으로 돈다.** 아래 "멈춰야 할 때"에 해당하지 않는 한 사용자에게 매 단계 확인을 구하지 않는다.

### 리더가 하지 않는 일

- **직접 구현 코드를 쓰지 않는다.** 리더의 컨텍스트는 조율에 쓴다. 예외: 진행 표 갱신, 커밋, 한두 줄짜리 병합 정리
- **자기가 스폰한 에이전트의 "완료했습니다"를 그대로 믿지 않는다.** 검증 에이전트의 판정만 신뢰한다
- **검증을 구현 에이전트에게 맡기지 않는다.** 자기 작업을 자기가 채점하면 통과한다

### 루프

```
   ┌──────────────────────────────────────────────┐
   │ 진행 표에서 다음 단계 판단                      │
   └───────────────────┬──────────────────────────┘
                       ▼
   ┌──────────────────────────────────────────────┐
   │ 구현 에이전트 스폰 (병렬 가능 시 동시)           │
   │   general-purpose · isolation: worktree        │
   └───────────────────┬──────────────────────────┘
                       ▼
   ┌──────────────────────────────────────────────┐
   │ 검증 에이전트 스폰 (별도 · 읽기 전용 지시)       │
   │   DoD 항목별 PASS / FAIL + 근거                │
   └───────────────────┬──────────────────────────┘
              FAIL ◄───┴───► PASS
                │              │
                ▼              ▼
   ┌────────────────────┐  ┌──────────────────────┐
   │ 수정 에이전트 스폰   │  │ 커밋 → 푸시 → main   │
   │ (FAIL 항목만 전달)   │  │ 병합 → 푸시 → 표 갱신 │
   └─────────┬──────────┘  └──────────────────────┘
             │ 최대 3회
             └──► 3회 초과 시 ⚠️ 로 표시하고 사용자에게 에스컬레이션
```

### 커밋 · 푸시

**기능 하나가 완료될 때마다 커밋 + 푸시까지 끝낸다.** 마일스톤을 몰아서 한 번에 올리지 않는다.

- **구현 에이전트** — 기능 단위로 자기 트랙 브랜치에 **커밋만** 한다. 푸시하지 않는다 (검증 전 코드다)
- **리더** — 검증 PASS 직후:

```powershell
git push -u origin track/<x>          # 트랙 브랜치
git checkout main
git merge track/<x>
git push origin main
```

- 검증 FAIL 상태의 코드는 푸시하지 않는다. 수정 후 PASS가 나면 그때 올린다
- 리모트가 설정돼 있지 않으면 푸시가 실패한다 — 사용자에게 알리고 커밋까지만 진행한 뒤 진행 표에 기록한다

### 에이전트 스폰

**구현 에이전트** — `subagent_type: "general-purpose"`. 파일을 동시에 고치는 병렬 트랙은 `isolation: "worktree"`를 붙인다.

프롬프트에 반드시 포함할 것:

```
저장소: C:\Projects\sticky-notes
먼저 CLAUDE.md, plan.md, process.md를 읽어라.

담당: 트랙 <X> — <마일스톤>
소유 파일: <process.md 소유권 표에서 그대로 복사>

규칙:
- 소유 파일 외에는 생성/수정하지 않는다
- 계약 파일 4개(lib.rs, ipc.ts, tauri.conf.json, tokens.css)는 절대 수정하지 않는다.
  부족하면 작업을 멈추고 무엇이 왜 필요한지 보고하라
- 색·치수·문구는 design/Sticky Notes for Windows.dc.html이 근거다. 임의로 정하지 않는다
- 완료 기준은 process.md의 "<마일스톤>" DoD 절이다. 전부 만족시켜라

보고: 무엇을 구현했는지, DoD 각 항목의 자체 판단, 막힌 것.
과장하지 말고 미완성은 미완성이라고 보고하라.
```

**검증 에이전트** — 별도 스폰. **구현 에이전트를 재사용하지 않는다.**

```
저장소: C:\Projects\sticky-notes
CLAUDE.md와 process.md를 읽어라.

너는 검증자다. 코드를 수정하지 마라. 판정만 하라.
트랙 <X> / <마일스톤>의 DoD 각 항목을 PASS / FAIL / 사람확인필요 로 판정하라.

반드시 직접 실행할 것:
  npm run build   npm test   cargo test   cargo clippy   (해당하는 것만)

코드 레벨로 확인할 것:
- todo!() · 더미 리턴 · 스텁 잔존 여부
- 계약 파일 4개가 diff에 들어갔는가 (들어갔으면 무조건 FAIL)
- 소유 파일 밖을 건드렸는가
- CLAUDE.md 절대 규칙 위반 (CDN 에셋, CodeMirror DOM 직접 조작, 하드코딩 색)
- 디자인 원본과 색·치수가 일치하는가

각 FAIL은 파일:줄 과 재현 방법을 포함하라.
"대체로 잘 됨" 같은 판정은 하지 마라. 항목별로 PASS 아니면 FAIL이다.
```

**수정 에이전트** — FAIL 항목만 전달한다. 마일스톤 전체를 다시 시키지 않는다.

```
저장소: C:\Projects\sticky-notes
CLAUDE.md를 읽어라.

검증에서 아래 항목이 FAIL 났다. 이것만 고쳐라. 범위를 넓히지 마라.

<FAIL 목록 — 파일:줄 + 재현 방법 그대로>

소유 파일: <동일>
계약 파일 4개는 수정 금지.
```

### 병렬 스폰 규칙

- **M0 이후 A · B · D를 한 메시지에서 동시에 스폰한다.** 소유 파일이 disjoint라 충돌하지 않는다
- **C는 M0 스파이크 결론이 채워진 뒤에만 스폰한다**
- 파일을 동시에 쓰는 병렬 에이전트는 `isolation: "worktree"`를 붙인다. 트랙별 브랜치명은 진행 표 참조
- 검증 에이전트도 트랙별로 병렬 스폰 가능하다
- **M0 · 통합 게이트 · M7은 직렬이다.** 병렬 스폰하지 않는다

### 멈춰야 할 때 — 사용자에게 에스컬레이션

리더가 자율 판단하지 않고 **반드시 사용자에게 묻는다**:

| 상황 | 이유 |
|---|---|
| 같은 항목이 **수정 3회 후에도 FAIL** | 접근 자체가 틀렸을 가능성. 더 돌려봐야 낭비 |
| 에이전트가 **계약 파일 변경을 요청** | 다른 트랙 전체에 영향. 아래 "계약 동결" 절차 |
| **M0 스파이크 실패** (투명 창 렌더링 불가) | 트랙 C의 구현 방식이 통째로 바뀐다 |
| **메모리 게이트 초과** (10개 창 > 300MB) | 열린 창 수 상한은 C·D에 영향 가는 설계 변경 |
| 사람 확인 항목이 쌓임 | 아래 참조 |

> **2026-07-26 사용자 지시 — 검증은 코드 레벨만.**
> 검증 에이전트가 **앱을 띄워 화면을 조작하는 검증(GUI 자동화·스크린샷·픽셀 측정)을 하지 않는다.**
> 허용: `npm run build` · `npm test` · `cargo test` · `cargo clippy` · grep · diff · 코드 리뷰.
> 이유: 앱 기동·합성 입력·픽셀 측정이 검증 시간의 대부분을 차지했다. 화면 동작은 사용자가 직접 테스트한다.
> 아래 표의 "사람" 열 항목은 전부 사용자 최종 테스트로 넘긴다.

### 사람만 확인할 수 있는 것 — 솔직한 한계

에이전트는 **빌드·테스트·코드 리뷰까지만** 검증할 수 있다. `plan.md`의 수동 체크리스트 12항목 중 상당수는 **눈으로 봐야 한다** — 에이전트가 통과 판정을 내릴 수 없다.

| 항목 | 에이전트 | 사람 |
|---|---|---|
| 1 다른 앱 위에 계속 보이는가 | ❌ | ✅ |
| 2 35%에서 뒤 창이 비치고 판독 가능한가 | ❌ | ✅ |
| 3 `Ctrl+Shift+휠` 증감·클램프 | 로직만 | ✅ |
| 4 포커스/블러 페이드가 **부드러운가** | 로직만 | ✅ |
| 5 유휴 전환에 레이아웃 시프트가 없는가 | ❌ | ✅ |
| 6 체크박스 즉시 렌더·클릭·영속 | 로직·테스트 | ✅ |
| 7 위키링크 마커 노출/숨김 | 로직·테스트 | ✅ |
| 8 멀티모니터 복원 | 왕복 테스트만 | ✅ |
| 9 **메모리 10개 ≤ 300MB** | ❌ | ✅ |
| 10 단축키 3개 · 트레이 6항목 | 등록 코드만 | ✅ |
| 11 보드 검색 3모드·필터·포커스 | 쿼리 테스트 | ✅ |
| 12 내보낸 md가 옵시디언에서 렌더 | 파일 생성만 | ✅ |

**리더는 이걸 매 마일스톤마다 사용자에게 묻지 않는다.** 진행 표에 🟡로 쌓아두고, 다음 시점에 묶어서 요청한다:

- **M0 직후** — 1, 2번. 스파이크 결론이 걸려 있어서 여기는 미룰 수 없다
- **통합 게이트** — 나머지 전부 + 9번 메모리

즉 사람이 앱을 직접 만져야 하는 지점은 **두 번**이다. 그 사이는 리더가 자율로 돈다.

---

## 병렬 작업 구조

**M0 이후 4트랙 병렬.** 트랙들이 겹치는 파일이 정확히 4개뿐이고, 그것들을 M0에서 동결하는 것이 병렬화의 전부다.

### 트랙 소유권

**자기 트랙이 소유하지 않은 파일은 건드리지 않는다.** 이게 병합 충돌을 막는 유일한 장치다. 구현 에이전트 프롬프트에 이 표의 해당 줄을 그대로 복사해 넣는다.

| 트랙 | 범위 | 단독 소유 파일 | 의존 |
|---|---|---|---|
| **A · 백엔드** | M2 데이터 레이어, 지오메트리, 내보내기 | `src-tauri/src/{db,notes,windows,win,export}.rs` | M0 계약만 |
| **B · 에디터** | M3 CodeMirror 라이브 프리뷰 | `src/editor/**`, `src/lib/markdown.ts`, `src/components/NoteEditor.tsx` | **없음** (순수 프론트) |
| **C · 메모 창** | M1 셸, M4 트레이·단축키 | `src/windows/NoteWindow.tsx`, `src/components/{ControlBar,ColorPalette,SaveFooter}.tsx`, `src-tauri/src/{tray,shortcuts}.rs` | M0 **스파이크 결론** |
| **D · 보드/설정** | M5, M6 | `src/windows/{BoardWindow,SettingsWindow}.tsx`, `src/components/{TitleBar,Toggle}.tsx`, `src/lib/time.ts` | M0 계약만 |

교차 참조는 단방향이고 M0 스텁이 흡수한다:

- C가 쓰는 `<NoteEditor value onChange />` → B가 완성할 때까지 textarea 스텁으로 동작
- A/C/D가 쓰는 모든 `invoke()` → A가 완성할 때까지 스텁이 더미 데이터 반환
- `TitleBar`/`Toggle`은 보드·설정에만 쓰인다 → **D 단독 소유**. 메모 창은 프레임리스라 `TitleBar`를 쓰지 않는다

### 알아둘 것

- **B가 단독 작업량 최대(~500-700줄)이면서 Tauri 의존이 0이다.** `npm run dev`만으로 검증이 끝나 에이전트가 가장 자율적으로 돌 수 있는 트랙이다
- A는 `cargo test`로 자체 검증되어 프론트 없이 완결된다 — 검증 에이전트가 판정하기 가장 쉽다
- **C는 M0 스파이크 결과에 종속적이다.** 투명 창 렌더링이 실패해 네이티브 폴백으로 가면 C의 투명도/그림자 구현이 통째로 바뀐다
- **직렬 구간은 두 곳뿐**: ① M0 전체 ② 통합 게이트

---

## 계약 파일 — 동결 규칙

```
src-tauri/src/lib.rs      — 모듈 선언 + invoke_handler 커맨드 등록
src/lib/ipc.ts            — 커맨드 TypeScript 타입 + invoke 래퍼
src-tauri/tauri.conf.json — 3종 창 설정
src/styles/tokens.css     — 디자인 토큰
```

> **2026-07-26 — 4트랙 병합 완료 시점에 동결 해제.** 병렬 트랙이 더 이상 없으므로 동결의 근거(트랙 간 계약 유지)가 사라졌다.
> 통합 게이트부터는 리더 승인 하에 계약 파일을 수정할 수 있다. 변경은 아래 "계약 변경 이력"에 계속 기록한다.

M0이 끝나면 이 4개는 **동결**된다. 어떤 에이전트도 임의로 수정하지 않는다. 검증 에이전트는 이 파일들이 diff에 들어갔으면 **무조건 FAIL**을 낸다.

### 계약 변경 이력

| 날짜 | 파일 | 변경 | 사유 |
|---|---|---|---|
| 2026-07-26 | `src/styles/tokens.css` | `--range-thumb-bg` `--range-thumb-border` **추가** | M0 검증에서 `global.css`의 슬라이더 thumb 하드코딩 색 2건 적발. 순수 추가라 기존 변수 불변, 트랙 스폰 **전**에 처리해 리베이스 비용 0 |
| 2026-07-26 | `src/styles/tokens.css` | `--dark-knob-on` `--on-accent` **추가** | 트랙 D 검증에서 `board.css`의 `#fff` 2건 적발. 흰색은 D뿐 아니라 **B(체크박스 마크 design:74,78,82,129)·C(메모 닫기 hover design:55)도 필요**하므로 세 트랙이 각자 하드코딩하기 전에 선제 추가. 순수 추가라 기존 변수 불변 |
| 2026-07-26 | `src/lib/ipc.ts` | `withFallback` 헬퍼 + 더미 데이터 + 호출 22곳 **삭제** | 통합 게이트 #1. 백엔드가 완성돼 폴백이 해로워졌다 — invoke 실패가 삼켜지고 화면에 더미가 떠서 디버깅이 불가능하다. 이제 실패는 `IpcError`로 던져지고 호출부가 한국어 배너로 노출한다 (`src/lib/errors.ts`) |
| 2026-07-26 | `src/lib/ipc.ts` | `Settings`에 `shortcutNewNote` `shortcutShowBoard` `shortcutToggleAlwaysOnTop` **추가**, `SHORTCUT_SETTING_KEY` 상수 추가 | 통합 게이트 #2. 단축키 재바인딩이 저장만 되고 읽히지 않아 재시작하면 사라졌다(게다가 key가 `shortcut.newNote`라 `put_setting`이 거부해 저장조차 안 됐다). `Settings` 필드로 만들어 쓰기·읽기를 한 경로로 맞물렸다. 순수 추가라 기존 필드 불변 |
| 2026-07-26 | `src/lib/ipc.ts` | `getShortcutFailures` 의미 확장 (등록 실패 + **기본값 폴백**) | 통합 게이트 #2. 저장된 단축키가 안 먹혀 기본값으로 되돌린 경우도 사용자에게 알려야 한다. 타입은 그대로(`ShortcutBinding[]`), 담기는 조건만 넓어졌다 |
| 2026-07-26 | `src-tauri/tauri.conf.json` | 메모 창 `width` 484→**436**, `height` 468→**420**, `minWidth` 288→**240**, `minHeight` 208→**160** | 사용자 결함 신고 #2 — "메모장 겉의 검정 레이아웃을 없애라". 창 = 종이 + 사방 24px 투명 여백 구조를 폐기하고 **창 = 종이**로 바꿨다. 여백 48px이 사라졌으므로 창 크기도 그만큼 줄인다. 근거·진단은 `src/styles/note.css` 머리말 |
| 2026-07-26 | `src/styles/tokens.css` | `--note-margin: 24px` **삭제** → `--note-grab: 6px` / `--note-grab-corner: 8px` **추가**, `--note-height: 420px` 추가 | 위와 같은 건. 리사이즈 그랩 존이 투명 여백에 있었으므로 종이 가장자리 **안쪽**으로 옮기면서 치수를 토큰화했다. `--shadow-note`는 정의만 남고 사용처 0 (디자인 기록용) |
| 2026-07-26 | `src-tauri/src/lib.rs` | `pub mod attachments;` + `attachments::save_attachment` `attachments::get_attachments_dir` **등록** | M7 이미지 첨부. 새 모듈이라 기존 커맨드 시그니처 불변 |
| 2026-07-26 | `src/lib/ipc.ts` | `saveAttachment()` `getAttachmentsDir()` **추가** (파일 끝) | 위 커맨드 2개의 래퍼. `saveAttachment`만 raw IPC 바디(`invoke(cmd, bytes, {headers})`)를 쓰므로 `call()`을 거치지 않는다 — 수 MB 스크린샷을 JSON 숫자 배열로 보내면 붙여넣기마다 멈춘다. 순수 추가 |
| 2026-07-26 | `src-tauri/tauri.conf.json` | `version` `0.1.0` → `1.0.0` | M7 정식 릴리스 전환. `package.json`·`Cargo.toml`도 동일. CI는 빌드 시 `1.0.<run_number>`를 주입한다 (plan.md "CI/CD") |
| 2026-07-26 | `src-tauri/src/lib.rs` | `run()` 을 `.run(context)` → `.build(context)?.run(closure)` 로 바꾸고 **`RunEvent::ExitRequested` 에서 `api.prevent_exit()`** | 사용자 신고 #2 — 메모 창을 전부 ✕로 닫으면 프로세스가 죽어 트레이 아이콘까지 사라졌다. tao 는 마지막 창이 destroy 되면 `ExitRequested { code: None }` 을 올리고 기본 동작으로 종료한다(`tauri-runtime-wry-2.11.4/src/lib.rs:4316`). `code` 가 실린 종료(트레이 `종료` → `app.exit(0)`, 같은 파일 `:4356`)만 통과시킨다 |
| 2026-07-26 | `src-tauri/src/lib.rs` | `pub mod update;` + `tauri_plugin_updater` 플러그인 + `update::{get_app_version, check_update, install_update, get_pending_update}` 등록 + `UpdateState` manage + `update::bootstrap` | 사용자 요청 #4 자동 업데이트. 새 모듈이라 기존 커맨드 시그니처 불변 |
| 2026-07-26 | `src-tauri/tauri.conf.json` | `plugins.updater`(endpoints · pubkey · `installMode: passive`) + `bundle.createUpdaterArtifacts: true` **추가** | 위와 같은 건. 엔드포인트는 `releases/latest/download/latest.json` — CI 가 만들어 릴리스에 붙인다. **개인키는 저장소에 없다**: GitHub Secrets `TAURI_SIGNING_PRIVATE_KEY` / `..._PASSWORD` |
| 2026-07-26 | `src/lib/ipc.ts` | `UpdateInfo` 타입 · `EVENT_NOTES_CHANGED` · `EVENT_UPDATE_AVAILABLE` · `getAppVersion` `checkUpdate` `getPendingUpdate` `installUpdate` **추가** | 위 커맨드/이벤트의 래퍼. 순수 추가라 기존 시그니처 불변 |
| 2026-07-26 | `src/styles/tokens.css` | `--attach-handle: 14px` / `--attach-handle-bg` **추가** | 사용자 요청 #5 이미지 크기 조절 손잡이. 순수 추가 |
| 2026-07-26 | `src/styles/tokens.css` | `--scrollbar-w: 8px` / `--scrollbar-thumb-w: 4px` / `--scrollbar-thumb-w-hover: 6px` **추가** | 스크롤바 축소 작업이 동결 규칙을 지키느라 `var(--scrollbar-w, 8px)` 폴백으로 돌려 뒀다 — 치수 3개가 `theme.ts`(종이)와 `board.css`(다크 크롬) **두 곳에 각각 박혀** 한쪽만 고치면 두 창의 스크롤바 굵기가 조용히 어긋난다. 토큰으로 올리고 폴백을 걷어내 근거를 한 곳으로 모았다. 순수 추가라 기존 변수 불변 |
| 2026-07-26 | `src/styles/tokens.css` | `--caret-w: 1.5px` / `--active-line-bg` / `--active-line-bg-focused` **추가** | 캐럿 재발 수정의 검증에서 **토큰 의미 오용 3건** 적발. 구현 에이전트가 동결 규칙을 지키느라 `--checkbox-border-w`(체크박스 테두리)를 캐럿 굵기로, `--footer-bg`(푸터 배경)·`--paper-divider`(본문 구분선)를 현재 줄 강조로 재사용했다 — 나중에 체크박스 테두리를 조정하면 캐럿 굵기가 조용히 따라 바뀐다. 리더가 순수 추가로 해소. 기존 변수 불변 |

### 계약을 바꿔야 할 때

1. 에이전트는 작업을 **멈추고** 리더에게 보고한다 (무엇이 왜 부족한지)
2. 리더는 **사용자에게 에스컬레이션한다** — 다른 트랙 에이전트가 동시에 돌고 있다
3. 승인되면 리더가 `main`에서 계약만 수정 → 커밋 `m0: 계약 변경 — <이유>`
4. 진행 중인 각 트랙 worktree를 `git rebase main`으로 갱신한다
5. 진행 표에 계약 변경을 기록한다

**절대 하지 말 것**: 에이전트가 자기 브랜치에서 계약 파일을 몰래 고치고 진행하기. 병합 시점에 다른 세 트랙이 전부 깨진다.

---

## 셋업

### 0. 저장소 초기화 (리더가 최초 1회, 직접)

```powershell
cd C:\Projects\sticky-notes
git init
git add .
git commit -m "docs: CLAUDE.md, plan.md, process.md, 디자인 원본"
```

### 1. M0 — 직렬, 에이전트 1개

`plan.md`의 M0 절을 그대로 수행시킨다. 병렬 스폰하지 않는다. 완료 후 리더가:

```powershell
git commit -m "m0: 스캐폴드 + 계약 스텁 + 디자인 토큰"
git push origin main
```

그리고 **진행 표 + 스파이크 결론 블록을 채운다.** 스파이크 1·2번은 사람 확인이 필요하므로 여기서 사용자에게 요청한다.

### 2. 트랙 병렬 실행

리더가 A · B · D 구현 에이전트를 **한 메시지에서 동시 스폰**한다 (`isolation: "worktree"`). 스파이크 결론이 나오면 C도 스폰한다.

`isolation: "worktree"`를 쓰면 worktree 생성·정리는 자동이다. 수동으로 할 경우:

```powershell
git worktree add ..\sn-b -b track/b-editor
cd C:\Projects\sn-b
npm install
```

> `node_modules`와 `src-tauri/target`은 worktree마다 독립이다. `target`은 수 GB가 될 수 있다. Rust를 안 쓰는 B/D는 `npm run dev`만 쓰면 `target`이 안 생긴다.

### 3. 병합

트랙이 검증 통과하면 리더가 `main`으로:

```powershell
git merge track/b-editor
```

파일 소유권이 disjoint라 계약 파일을 건드리지 않았다면 충돌이 나지 않는다. **충돌이 났다면 소유권 규칙이 깨진 것이므로, 자동 해결하지 말고 무엇이 어긋났는지 먼저 확인한다.**

---

## 완료 기준 (Definition of Done)

검증 에이전트는 아래 항목을 **하나씩 PASS/FAIL로** 판정한다. "대체로 잘 됨"은 판정이 아니다.

### M0

- [ ] `npm run tauri dev`로 빈 창 3종이 뜬다 (`?w=note` / `?w=board` / `?w=settings`)
- [ ] 계약 파일 4개가 채워져 있고 `npm run build` + `cargo check` 통과
- [ ] `design/Sticky Notes for Windows.dc.html`이 저장돼 있다
- [ ] 폰트가 로컬 번들이고 네트워크를 끊어도 렌더된다
- [ ] **스파이크 결론 블록이 채워져 있다** ← 트랙 C 차단 해제 조건
- [ ] 🟡 사람 확인: 1번(항상 위), 2번(투명도 35%)

### CI — M0 병합 직후 (리더 관장)

- [ ] `.github/workflows/release.yml`이 `push: branches: [main]`에서 돈다
- [ ] `windows-latest`에서 `npm ci` → `npm run build` → `npm run tauri build` 통과
- [ ] 태그 `v0.1.<run_number>` 생성 + NSIS 인스톨러가 릴리스 자산으로 첨부된다
- [ ] v1.0.0 전까지 `prerelease: true`
- [ ] 실제 푸시로 워크플로가 성공하고 릴리스 자산을 내려받을 수 있다 (`gh run list` / `gh release view`로 확인)

상세 사양은 `plan.md`의 "CI/CD" 절.

### M1 — 트랙 C

- [ ] 5색 전환, 컨트롤 바 5개 컨트롤 전부 동작
- [ ] 유휴 상태에서 **레이아웃 시프트 없이** 컨트롤이 사라진다
- [ ] 드래그 · 리사이즈 · `Ctrl+Shift+휠`
- [ ] 🟡 사람 확인: 3, 4, 5번

### M2 — 트랙 A

- [ ] `cargo test` 통과 — 마이그레이션 멱등성, `save_note` 트랜잭션, 검색 3모드, 지오메트리 왕복
- [ ] `cargo clippy` 경고 없음
- [ ] 제목 파생이 엣지케이스를 처리한다 (빈 본문 / 코드블록으로 시작 / 제목만)
- [ ] 🟡 사람 확인: 8번(멀티모니터 복원) — 프론트 통합 후

### M3 — 트랙 B

- [ ] `npm test` 통과 — 특히 **코드블록 내부의 `#태그`·`[[링크]]`가 추출되지 않아야 함**
- [ ] 체크박스 클릭이 **트랜잭션 dispatch**로 구현돼 있다 (DOM 직접 조작이면 FAIL)
- [ ] 커서가 노드 안이면 마커 노출, 밖이면 숨김 — 4종 전부 (`**` / `` ` `` / `#` / `[[ ]]`)
- [ ] 마크다운 원문 무손실: 긴 문서를 붙여넣고 전체 복사 → 원문과 바이트 동일 (테스트로 검증)
- [ ] 🟡 사람 확인: 6, 7번

### M4 — 트랙 C

- [ ] 트레이 메뉴 6항목 전부 동작, 좌클릭 → 보드 토글
- [ ] 전역 단축키 3개
- [ ] **단축키 등록 실패가 사용자에게 보인다** (조용한 무동작이면 FAIL)
- [ ] 중복 실행 시 기존 인스턴스가 깨어난다
- [ ] 🟡 사람 확인: 10번

### M5 — 트랙 D

- [ ] 검색 3모드 (일반 / `#태그` / `[[백링크]]`)
- [ ] 색상 필터, 카드 클릭 → 해당 메모 창 포커스, 우클릭 → 삭제
- [ ] 리사이즈 중 mica 플래시 없음 (있으면 폴백 적용 후 이 파일에 기록)
- [ ] 🟡 사람 확인: 11번

### M6 — 트랙 D

- [ ] DISPLAY / DATA / SHORTCUTS 3개 섹션
- [ ] 마크다운 내보내기가 실제 파일을 생성한다
- [ ] 🟡 사람 확인: 12번(옵시디언에서 렌더)

### 통합 게이트 ← **직렬. 리더가 직접 관장한다**

- [ ] 4트랙 병합 후 `npm run build` + `cargo test` + `cargo clippy` 통과
- [ ] 스텁이 전부 실제 구현으로 교체됐다 (`todo!()` · 더미 리턴 · textarea 스텁 잔존 0)
- [ ] 🟡 **사람 확인: 수동 체크 12항목 전부**
- [ ] 🟡 **사람 확인: 메모리 게이트 — 메모 창 10개 ≤ 300MB** (작업 관리자, 전 프로세스 합산)

메모리 게이트 실패 시: 열린 메모 창 수에 상한을 두고 나머지는 보드에서만 보이게 한다. C·D에 영향 가는 설계 변경이므로 **사용자에게 먼저 알린다.**

### M7

- [ ] 이미지 붙여넣기 → 저장 → 인라인 렌더
- [ ] `npm run tauri build`로 NSIS 인스톨러 생성
- [ ] 자동 시작 토글
- [ ] 🟡 사람 확인: 클린 설치 후 동작

---

## 세션 인계 절차

리더 세션이 끝나거나 중단될 때 **반드시**:

1. 진행 중이던 에이전트 결과를 커밋한다 (`CLAUDE.md`의 커밋 규약). 검증 PASS 상태면 푸시까지 끝낸다
2. **진행 표를 갱신한다** — 상태 기호, 날짜, 부분 완료면 남은 것
3. 🟡 사람 확인 대기 항목이 있으면 무엇인지 적는다
4. 막혔으면 ⚠️로 표시하고 아래에 무엇이 왜 막혔는지 3줄 이내로 적는다
5. 계약 파일이 바뀌었다면 눈에 띄게 적는다

다음 리더 세션은 진행 표만 보고 이어받을 수 있어야 한다.

### 통합 게이트 처리 목록 — ✅ 4건 전부 처리 완료 (2026-07-26, 검증 PASS)

> 트랙 검증 과정에서 발견됐지만 **단일 트랙의 FAIL 사유가 아닌** 것들. 통합 게이트에서 리더가 처리한다.
>
> **처리 결과 요약**
> 1. `withFallback` 22곳 + 더미 4종 제거 → `errors.ts` 신설, 전 창에 한국어 에러 배너. 보드 삭제 실패 시 카드가 지워지던 버그도 함께 수정
> 2. 단축키 영속화 — **원인이 2개였다.** 읽는 경로 부재 + 저장 key가 `shortcut.newNote`(점)라 `put_setting`이 거부하고 `eprintln!`으로 삼키던 것. key를 camelCase로 바꾸고 `Settings`에 3필드 추가. 저장값 우선 등록 → 실패 시 기본값 폴백 + 사유 노출. **잘못된 단축키를 DB에 주입해 재시작하는 실측으로 폴백·배너까지 확인됨**
> 3. `after_space_indent()`로 탭 들여쓰기 계산 수정 — `fence_info`·`is_closing_fence`에 더해 같은 버그가 있던 `heading_text`(ATX 제목)도. **12케이스 파리티 실측 12/12 일치**
> 4. `deriveTitle`/`derivePreview` 삭제 (호출처 0건 + Rust와 상수·규칙 불일치). 진실의 원천은 Rust
>
> 추가 처리: 죽은 폴백(`shortcuts::new_note`의 임시 uuid) 제거, 단축키 실패 노출 경로 2개(트레이 툴팁 + 메모 0개 시 설정 창) 추가

| # | 항목 | 근거 |
|---|---|---|
| 1 | **`ipc.ts`의 `withFallback` 21곳 제거** | 백엔드 미완 시절의 더미 반환. 트랙 A 병합으로 실데이터가 흐르므로 제거해야 한다. `withFallback` grep → 0이 되어야 함. **계약 파일 수정이므로 리더가 한다** |
| 2 | **단축키 재바인딩이 재시작 후 사라진다 — 계약 공백** | `set_shortcut`이 `db::set_setting`으로 쓰지만 **읽는 경로가 없다.** `Settings` 구조체에 단축키 필드가 없고 임의 키를 읽는 커맨드도 없어 `shortcuts::init`이 항상 기본값을 등록한다. `Settings`에 필드 추가 또는 `get_setting(key)` 커맨드 신설 필요 (계약 변경) |
| 3 | **탭 들여쓰기 펜스에서 Rust/TS 규칙 불일치** (M2부터 존재, 이번 회귀 아님) | `notes.rs`의 `fence_info`가 들여쓰기를 `line.len() - trim_start().len()`으로 재는데 `trim_start()`가 탭도 걷어내 탭 1개를 "3칸 이하"로 인정한다. 프론트 `FENCE_OPEN_RE`는 스페이스만 허용(CommonMark 기준으로 프론트가 옳다). 불일치 입력: `"\t```\n#진짜태그"`, `"```\n#코드안\n\t```\n#바깥태그"`, `"\t~~~\n#태그티엘"` |
| 4 | `markdown.ts`의 `deriveTitle`/`derivePreview`가 데드코드 | 현재 호출처 0건이라 무해하나, 나중에 연결하면 Rust와 불일치가 화면에 뜬다. `derivePreview` 기본 `maxLength=120` vs Rust `PREVIEW_MAX_CHARS=160`도 어긋나 있다 |

### 🟡 누적 사람 확인 항목

> 사용자 지시로 중간 확인은 생략한다. **통합 게이트에서 한 번에** 확인한다.
> `plan.md` 수동 체크리스트 12항목에 더해, 검증 과정에서 코드로 판정 불가로 밝혀진 것들:

| 출처 | 항목 |
|---|---|
| M0 스파이크 4 | 메모 창 드래그·리사이즈 중 깜빡임 정도 |
| M5 DoD | 보드 창 리사이즈 중 mica 검은 플래시 (폴백 체인·CSS 방어는 구현됨, 실제 발생 여부만 미확인) |
| 트랙 D 검증 | 보드 창 라운드 코너 — `transparent:false`인데 `border-radius:10px`라 코너 4개 삼각형에 창 배경이 비칠 수 있음. opaque 폴백 시엔 색이 같아 무해하나 mica/acrylic 경로는 실행해 봐야 안다 |

### 사용자 신고 5건 — 2026-07-26 처리 내역

| # | 신고 | 원인 | 처리 |
|---|---|---|---|
| 1 | 메모를 클릭해도 커서가 안 깜빡인다 | `drawSelection()` 캐럿은 **에디터가 포커스일 때만** 그려진다(`.cm-focused` 안에서만 `cm-blink`). 창만 활성화되고 포커스가 `<body>` 에 남으면 캐럿이 없다. 게다가 종이 본문 여백 mousedown 은 `startDragging()` 이라 눌러도 커서가 안 들어갔다 | `NoteEditor` 에 `focus/focusEnd/focusAt/hasFocus` 핸들 노출 · 창 생성 시 `autoFocus` · 창 포커스마다 에디터로 커서 복귀 · **본문 여백 클릭 = 캐럿 배치**(드래그 아님). 대신 컨트롤 바·푸터의 드래그 영역을 넓혔다 |
| 2 | 메모를 전부 닫으면 프로세스가 죽는다 | tao 가 마지막 창 destroy 에 `ExitRequested { code: None }` → 기본 종료 | `lib.rs` 에서 `code.is_none()` 이면 `prevent_exit()`. 트레이 `종료`(`code: Some(0)`)만 실제로 끝난다 |
| 3 | 보드 창에서 추가·열기·삭제가 안 된다 | **바뀜을 알리는 쪽이 없었다.** `sticky://note-meta-changed` 는 `shortcuts.rs` 전역 핀 토글에서만 emit 됐고 create/save/set_meta/soft_delete 는 아무 이벤트도 안 냈다 → 보드는 처음 읽은 목록을 끝까지 들고 있었다. 보드에는 **새 메모 버튼 자체가 없었고**, `open_note_window` 는 `notes.open` 을 1로 만들지 않았다 | `sticky://notes-changed` 신설 + 위 4개 커맨드와 창 열기/닫기에서 emit · 보드가 이벤트·창 포커스 양쪽에서 재조회 · 보드 툴바에 `+ 새 메모` · `open_note_window` 가 `open=1` 기록 · 삭제 시 그 메모의 창도 destroy · 우클릭 메뉴 닫기를 `stopPropagation` 대신 target 검사로 · **창을 만들거나 없애는 커맨드 전부 `#[tauri::command(async)]`** (동기 커맨드는 메인 스레드에서 돌아 웹뷰 IPC 콜백 안에서 WebView2 를 만들게 된다) |
| 4 | 자동 업데이트 | — | `tauri-plugin-updater`. 확인·다운로드·설치는 전부 Rust(`update.rs`) — 웹뷰 CSP 를 열지 않아도 된다. 시작 4초 뒤 1회 자동 확인 → 메모 창 배너 · 설정 창 `UPDATE` 섹션 |
| 5 | 이미지 마우스로 크기 조절 | — | 위젯 우하단 손잡이. 끄는 동안은 상자의 인라인 `width` 만 바꾸고(미리보기), **손을 뗄 때 트랜잭션 1회**로 `![|400](…)` 를 쓴다(절대규칙 4). 문법은 옵시디언과 동일 — 내보낸 `.md` 가 볼트에서 그대로 렌더돼야 한다 |

### 신고 #1 재발 — 2026-07-26 재수정 (`fix/caret-visibility`)

위 표 #1의 수정은 **불충분했다.** 복구 경로가 Tauri `onFocusChanged` 하나뿐이었는데, 다음은 **창 활성 상태가 바뀌지 않아** 그 경로가 아예 걸리지 않았다:

| 경로 | 왜 캐럿이 죽나 |
|---|---|
| 핀·색상 버튼 클릭 | `activeElement` = 버튼. `focusEditor()` 의 가드(`active.closest('.note-controls')`)가 **정의상** 복구를 막았다 |
| 컨트롤 바·푸터 드래그, 가장자리 리사이즈 | 네이티브 모달 루프가 도는 동안 **창은 활성 상태 그대로**이고 웹뷰만 포커스를 잃는다 → `onFocusChanged` 가 안 온다 |
| 배너 닫기 | 배너 언마운트 → 포커스가 `<body>` 로 |

근거(`@codemirror/view` 6.43.6): `drawSelection()` 이 `Prec.highest` 로 네이티브 캐럿을 죽이므로(`dist:9589`) 화면의 캐럿은 `.cm-cursor` div 하나뿐이고, 그건 `view.hasFocus`(`dist:8590` — `document.hasFocus() && root.activeElement == contentDOM`)에 100% 종속이다.

처리: 가드 없는 `returnFocusToEditor()` 를 위 경로 전부에 연결 · 웹뷰 DOM `window` focus 도 청취(네이티브 모달 루프 종료를 알 수 있는 유일한 경로) · **`highlightActiveLine()` 추가** — 캐럿은 포커스 없이는 존재할 수 없으므로, 포커스와 무관하게 남는 현재 줄 표시가 "어느 라인인가"를 보장한다.

**검증에서 드러난 한계 (사용자 실기 확인 필요)**: jsdom 에 레이아웃이 없어 `.cm-cursor` 엘리먼트가 테스트에서 **단 하나도 생성되지 않는다**(`RectangleMarker.forRange` 가 `getClientRects` 에서 죽는다). 즉 어떤 테스트도 "캐럿이 보인다"를 보장하지 못한다. 실질 수정처인 `NoteWindow.tsx` 의 포커스 회수 로직도 커버리지 0이다.

**#4 는 사용자 조치가 남아 있다** — GitHub 저장소 Secrets 에 서명 키 2개를 등록해야 CI 가 통과한다. 등록 전까지 `release.yml` 의 "서명 키 확인" 단계에서 명시적으로 실패한다(조용히 서명 없는 인스톨러를 내보내지 않는다).

### 막힘 기록

<!-- ⚠️ 상태인 단계는 여기에 기록. 해결되면 지운다. -->

_(없음)_
